<%= form_with(model: tweet, local: true) do |form| %>
  <div class="field">
    <%= form.text_area :body, id: :tweet_body, class:'form-control' %>
  </div>

  <div class="actions text-right">
    <%= form.submit :class => 'btn btn-info' %>
  </div>
<% end %>