<nav class="navbar navbar-inverse">
    <div class="container-fluid">
        <div class="navbar-header">
            <a class="navbar-brand" href="#">Analiz & Sınıflandırma</a>
        </div>
        <ul class="nav navbar-nav">
            <li class="active"><a href="#">Anasayfa</a></li>
            <li>
                <form class="navbar-form navbar-left" action="#">
                    <div class="input-group">
                        <input type="text" class="form-control baslik" placeholder="Ara...">
                        <div class="input-group-btn">
                            <button class="btn btn-default" type="submit">
                                <i class="glyphicon glyphicon-search"></i>
                            </button>
                        </div>
                    </div>
                </form>
            </li>
        </ul>
        <ul class="nav navbar-nav navbar-right">
            <% if !current_user %>
                <li><a href="<%= root_url+'auth/twitter' %>"><span class="glyphicon glyphicon-log-in"></span> Giriş Yap</a></li>
            <% else %>
                <li><a href="<%= root_url+'sessions/logout' %>"><span class="glyphicon glyphicon-log-out"></span> Çıkış Yap(<%= current_user.name %>)</a></li>
            <% end %>
        </ul>
    </div>
</nav>